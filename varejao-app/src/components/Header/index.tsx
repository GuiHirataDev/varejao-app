import { Header } from "./styles"

export const HeaderComponent = () => {
    return (
        <Header>
            <p className="logo">logo</p>
            <button>lll</button>
        </Header>
    )
}